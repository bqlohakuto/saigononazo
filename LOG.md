# LOG機能

AUTOの右隣のLOGで、現在の会話ウィンドウを上へ拡張します。もう一度LOG、またはEscapeで閉じます。開くたびに最下部から表示し、上へスクロールして履歴を読みます。話者名の表示は増やさず、本編の文字色・吹き出しを引き継ぎます。

LOG中は次へ・AUTOを停止します。文字送り中に開いた場合は現在の1文だけ全文表示にし、次の文へは進めません。閉じると元の会話を表示し、AUTOがONだった場合は改めて2秒待ってから再開します。

## 記録とID

- `scenario.js` と `script.js` の文章に、本文から生成しない固定の `logId` と `logType` を指定しています。
- 会話の共通処理 `expandScenario()` が、元のIDに表示単位の枝番 `_01`、`_02` …を付けます。例：`room1_hana_first_01_01`。
- `Dialogue.start()` の `onDisplay` から `recordLog()` を呼び、表示を開始した1文を記録・保存します。まだ表示していない次の文は記録しません。
- メールは本文を開いた時点で、送受信情報・時刻・件名・本文を1件として記録します。探索の案内、ピアノの結果も `recordLog()` / `showLoggedText()` を使います。ボタン名などの操作ラベルは対象外です。
- `GameLog` の `Set` がIDの既読状態を管理し、同じIDは初回のみ保存します。同じ本文でも別IDなら別件になります。
- 同じ文章を再表示している場合、保存済み履歴の位置は変えず、必要に応じて末尾に「現在のテキスト」を一時表示します。この現在文の表示は履歴へ追加・保存されません。

既存のIDは並び替えても変更・再利用しないでください。既存文章の途中に表示単位を追加・削除すると枝番が変わるため、その場合は文章を別の固定IDの要素として追加するか、改訂した文章に新しいIDを割り当ててください。

## 保存形式

保存先は既存の `saigononazo-save-v1` のままです。`playerName`、第一の部屋の `scene` / `state` は維持し、トップレベルに `logs` 配列を追加しています。

```js
{
  id: "room1_hana_first_01_01",
  order: 42,                   // 0からの表示順。配列もこの順序
  text: "あの……えっと……あなたは……",
  type: "dialogue",            // dialogue / investigation / narration
  speaker: "主人公",
  thought: false,
  kind: "player"               // 本編の表示クラス
  // color: "#5f675a"          // 探索案内など個別の文字色がある場合
}
```

オープニング中の履歴も保存するため、`scene: "opening"` と `openingIndex`（現在表示している1文の位置）も扱います。再読み込み後は「つづきから」で復元します。第一の部屋での再開位置は従来通りです。

古いセーブに `logs` がなければ空の履歴として読み込み、その後表示した文章から記録します。過去に読んだ文章を推測して補完しません。「はじめから」で名前を決定すると既存セーブとLOGを初期化します。

## 将来のタブ追加

`GameLog.list()` が返す配列を `entry.type` で絞り込めます。種類は表示色と独立しており、主人公の内面は青い表示のまま `narration`、携帯を調べる際の主人公の文章は `investigation` です。今回は全種類を時系列で表示し、タブ・検索などは実装していません。

## 実プレイでの確認項目

- iPhoneの縦・横画面で上へ拡張し、画面内に収まること。指でスクロールでき、AUTO / LOG / 次へが押せること。
- 開くたびに現在文が最下部にあり、閉じると通常のサイズ・本文に戻ること。
- AUTO ONや文字送り中に開いても、読んでいる間は次の文へ進まないこと。閉じた後にAUTOが再開すること。
- ハナ・扉・ポスター・メールなどを再度表示しても履歴が蓄積しないこと。色・改行・記録対象を確認すること。
- 再読み込み後の「つづきから」で履歴が復元されること。オープニング中の再開、古いセーブ、はじめからの初期化も確認すること。

Codex側ではブラウザ操作、通しプレイ、スクリーンショット確認、ゲーム進行テストは行わず、構文・参照・LOGデータの最小限の確認だけを実施します。
