# LOG機能

AUTOの右隣のLOGで、現在の会話ウィンドウを上へ拡張します。もう一度LOG、またはEscapeで閉じます。開くたびに最下部から表示し、上へスクロールして履歴を読みます。話者名の表示は増やさず、本編の文字色・吹き出しを引き継ぎます。

LOG中は次へ・AUTOを停止します。文字送り中に開いた場合は現在の1文だけ全文表示にし、次の文へは進めません。閉じると元の会話を表示し、AUTOがONだった場合は改めて2秒待ってから再開します。

## 記録とID

- `scenario.js` と `script.js` の文章に、本文から生成しない固定の `logId` と `logType` を指定しています。
- 会話の共通処理 `expandScenario()` が、元のIDに表示単位の枝番 `_01`、`_02` …を付けます。例：`room1_hana_first_01_01`。
- `logId` は「どの文章・表示元か」を示す固定IDです。履歴の1回ごとの表示を一意にするIDではありません。同じ文章を再表示した場合は、同じ `logId` の履歴が複数件並びます。
- `Dialogue.start()` の `onDisplay` から `recordLog()` を呼び、表示を開始した1文をその都度記録・保存します。まだ表示していない次の文は記録しません。
- メールは本文を開くたびに、送受信情報・時刻・件名・本文を1件として記録します。探索の案内、ピアノの結果も `recordLog()` / `showLoggedText()` を使い、同じ内容を再度表示した場合もその都度履歴へ追加します。ボタン名などの操作ラベルは対象外です。
- 表示順は `order` と配列順で管理します。同じ `logId`・同じ本文でも、再表示された回数だけ別の履歴として末尾へ追加します。
- LOGを開いた時点の現在文は通常すでに履歴末尾に記録されています。何らかの理由で現在文が履歴にまだ存在しない場合だけ、「現在のテキスト」として一時表示します。この一時表示自体は履歴へ追加・保存しません。

既存の `logId` は文章の参照元を識別するため、並び替えても変更・再利用しないでください。既存文章の途中に表示単位を追加・削除すると枝番が変わるため、その場合は文章を別の固定IDの要素として追加するか、改訂した文章に新しいIDを割り当ててください。

## 保存形式

保存先は既存の `saigononazo-save-v1` のままです。セーブデータ内部は `saveVersion: 2` を持ち、現在位置は `currentScene`、各部屋の状態は `rooms` に保存します。LOGは引き続きトップレベルの `logs` 配列です。詳細は `docs/SAVE_FORMAT.md` を参照してください。

```js
{
  id: "room1_hana_first_01_01", // 表示元を示す固定ID。再表示時は同じIDで別件が追加される
  order: 42,                   // 0からの表示順。配列もこの順序
  text: "あの……えっと……あなたは……",
  type: "dialogue",            // dialogue / investigation / narration
  speaker: "主人公",
  thought: false,
  kind: "player"               // 本編の表示クラス
  // color: "#5f675a"          // 探索案内など個別の文字色がある場合
}
```

オープニング中は `currentScene: "opening"` と `opening.index`、第一の部屋では `currentScene: "room01"` と `rooms.room01` を使って再開します。旧形式の `scene` / `state` / `openingIndex` は読み込み時にv2へ正規化されます。

古いセーブに `logs` がなければ空の履歴として読み込み、その後表示した文章から記録します。既存セーブの `logs` 配列は配列順のまま復元し、同じIDが複数含まれている場合も削除しません。過去に読んだ文章を推測して補完しません。「はじめから」で名前を決定すると既存セーブとLOGを初期化します。

## 将来のタブ追加

`GameLog.list()` が返す配列を `entry.type` で絞り込めます。種類は表示色と独立しており、主人公の内面は青い表示のまま `narration`、携帯を調べる際の主人公の文章は `investigation` です。今回は全種類を時系列で表示し、タブ・検索などは実装していません。

## 実プレイでの確認項目

- iPhoneの縦・横画面で上へ拡張し、画面内に収まること。指でスクロールでき、AUTO / LOG / 次へが押せること。
- 開くたびに現在文が最下部にあり、閉じると通常のサイズ・本文に戻ること。
- AUTO ONや文字送り中に開いても、読んでいる間は次の文へ進まないこと。閉じた後にAUTOが再開すること。
- ハナ・扉・ポスター・メールなどを同じ条件で複数回表示し、その回数分だけ同じ `logId` の履歴が時系列で蓄積すること。色・改行・記録対象を確認すること。
- 再読み込み後の「つづきから」で、重複を含む履歴が表示順どおり復元されること。オープニング中の再開、古いセーブ、はじめからの初期化も確認すること。

Codex側ではブラウザ操作、通しプレイ、スクリーンショット確認、ゲーム進行テストは行わず、構文・参照・LOGデータの最小限の確認だけを実施します。
