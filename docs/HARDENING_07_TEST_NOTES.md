# Stabilization 07 — 手動確認

1. 受信BOXと送信BOXの全メールを通常どおり開き、正しいピアノ入力で従来どおり進めること。
2. 通常プレイではメール条件の体感が変わっていないこと。
3. オープニングのハナの表示・吹き出し・色が従来どおりであること。
4. 更新後のSafariで古い `stable06` のJS/CSSが混在せず、`stable07` が読み込まれること。

自動テストでは、正しい全IDなら既読完了、同じ件数でも不正IDが混ざる場合は未完了、未知のフォルダは未完了、`scenario.js` に `displayName` が残らないことを確認します。
